diff --git a/src/spatialjoin/SpatialJoinMain.cpp b/src/spatialjoin/SpatialJoinMain.cpp
index 368cce8..f0bab1f 100755
--- a/src/spatialjoin/SpatialJoinMain.cpp
+++ b/src/spatialjoin/SpatialJoinMain.cpp
@@ -409,7 +409,7 @@ int main(int argc, char** argv) {
         exit(1);
 #endif
       } else {
-        int f = open(inputFiles[i].c_str(), O_RDONLY);
+        int f = open(inputFiles[i].c_str(), O_RDONLY | O_BINARY);
 
         if (f < 0) {
           std::cerr << "Could not open input file " << inputFiles[i]
diff --git a/src/spatialjoin/Sweeper.cpp b/src/spatialjoin/Sweeper.cpp
index 8032588..2261ea0 100644
--- a/src/spatialjoin/Sweeper.cpp
+++ b/src/spatialjoin/Sweeper.cpp
@@ -1,3 +1,7 @@
+#ifdef _WIN32
+#include <io.h>
+#endif
+
 #include <errno.h>
 #include <stdio.h>
 #include <unistd.h>
@@ -1094,7 +1098,7 @@ void Sweeper::flush() {
   log("Sorting events...");
 
   std::string newFName = util::getTmpFName(_cache, ".spatialjoin", "sorttmp");
-  int newFile = open(newFName.c_str(), O_RDWR | O_CREAT, 0666);
+  int newFile = open(newFName.c_str(), O_RDWR | O_CREAT | O_BINARY, 0666);
   unlink(newFName.c_str());
 
   if (newFile < 0) {
@@ -1115,7 +1119,11 @@ void Sweeper::flush() {
     throw std::runtime_error(ss.str());
   }
 
+#ifdef _WIN32
+  _commit(newFile);
+#else
   fsync(newFile);
+#endif
 
   close(_file);
 
diff --git a/src/spatialjoin/Sweeper.h b/src/spatialjoin/Sweeper.h
index c95fc91..a434b0d 100644
--- a/src/spatialjoin/Sweeper.h
+++ b/src/spatialjoin/Sweeper.h
@@ -4,6 +4,12 @@
 #ifndef SPATIALJOINS_SWEEPER_H_
 #define SPATIALJOINS_SWEEPER_H_
 
+// Files written by the sweeper are binary; on Windows, open() defaults to
+// text mode unless O_BINARY is given. POSIX systems have no O_BINARY.
+#ifndef O_BINARY
+#define O_BINARY 0
+#endif
+
 #include <fcntl.h>
 #include <stdio.h>
 #include <stdlib.h>
@@ -229,7 +235,7 @@ class Sweeper {
 
     // OUTFACTOR 1
     _fname = util::getTmpFName(_cache, tmpPrefix, "events");
-    _file = open(_fname.c_str(), O_RDWR | O_CREAT | O_TRUNC, 0666);
+    _file = open(_fname.c_str(), O_RDWR | O_CREAT | O_TRUNC | O_BINARY, 0666);
 
     if (_file < 0) {
       throw std::runtime_error("Could not open temporary file " + _fname);
